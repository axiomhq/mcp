{
  "name": "axiom-mcp-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/mcp",
    "deploy": "npm run deploy --workspace=apps/mcp",
    "lint": "npm run lint --workspaces",
    "type-check": "npm run type-check --workspaces",
    "test": "npm run test --workspaces",
    "test:watch": "npm run test:watch --workspaces"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.6",
    "@types/node": "^24.0.7",
    "typescript": "^5.8.3",
    "ultracite": "5.0.26"
  }
}

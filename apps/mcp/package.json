{
  "name": "axiom-mcp-app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:prod-us": "wrangler deploy --env prod",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "lint": "ultracite format",
    "gen:types": "wrangler types",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest --watch"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.0.5",
    "@microlabs/otel-cf-workers": "^1.0.0-rc.52",
    "@modelcontextprotocol/sdk": "^1.13.0",
    "@watchly/mcp": "file:../../packages/mcp",
    "agents": "^0.0.95",
    "hono": "^4.8.2",
    "just-pick": "^4.2.0",
    "workers-mcp": "^0.0.13",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "vitest": "^3.2.4",
    "wrangler": "^4.22.0"
  }
}
